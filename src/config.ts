// Default paths and constants
export const DEFAULT_CONTEXT_FILE_PATH = 'context.json'; 

// Default analysis query when none is provided by the user
export const DEFAULT_ANALYSIS_QUERY = 'Analyze the input files and provide a comprehensive overview of the system architecture, including main components, design patterns, and key relationships between modules.'; 